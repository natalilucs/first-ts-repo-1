// //ex03

export function calcVolume(raioBase: number, alturaCilindro: number):number {
    let volumeCilindro;
    volumeCilindro = (raioBase * raioBase) * 3.14 * alturaCilindro;
    return volumeCilindro;
}

